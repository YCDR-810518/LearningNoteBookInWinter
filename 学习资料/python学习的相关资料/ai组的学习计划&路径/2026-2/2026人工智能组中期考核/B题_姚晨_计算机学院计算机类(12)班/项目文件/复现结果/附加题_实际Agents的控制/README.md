# 附加题复现结果查看指南
### 文件夹
* 无异常数据注入的MP4视频复现结果
  * 使用的最大隐私允许值为与论文值保持一致
    * `eps_max = np.array([0.4, 0.9, 0.55, 0.35, 0.8, 0.45, 0.7, 0.5, 0.52, 0.58])`
* 具有异常注入的的MP4视频
  * 对比不同最大隐私值下的迭代变化
  * MP4视频结尾数值的为该视频中所使用的最大隐私值
  * 数值包括
    * `epsilon_scales = [0.1, 0.5, 1.0, 10.0]`
* 稳定度&攻击注入水平对比
  * 四根线都是存在的!只是后面已经找到最优解了!
  * 隐私预算为0.1时不够,导致没有收敛为圆形!